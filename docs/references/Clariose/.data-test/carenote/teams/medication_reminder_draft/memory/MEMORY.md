# Medication Schedule Draft Agent — memory index

Long-term notes this role accumulates across visits.
Each entry is one short line; topics live as their own `<topic>.md` files alongside this index when they grow beyond a sentence.

<!-- entries appended by RoleWorkspaceService.appendMemoryNote -->
