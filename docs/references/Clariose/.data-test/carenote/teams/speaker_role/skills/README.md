# Skills — Speaker Role Agent

Drop `SKILL.md` files in this directory to give this role private skills it can call. Other roles never see this folder.

Layout: `skills/<skill-name>/SKILL.md` (front-matter: `name`, `description`, `when_to_use`).
